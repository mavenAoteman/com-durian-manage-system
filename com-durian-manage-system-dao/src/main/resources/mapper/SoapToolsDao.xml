<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.durian.manage.system.dao.SoapToolsDao">

    <resultMap type="com.durian.manage.system.domain.SoapTools" id="SoapToolsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="ownerUserId" column="owner_user_id" jdbcType="BIGINT"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="brand" column="brand" jdbcType="VARCHAR"/>
        <result property="quantity" column="quantity" jdbcType="INTEGER"/>
        <result property="unitPrice" column="unit_price" jdbcType="NUMERIC"/>
        <result property="totalPrice" column="total_price" jdbcType="NUMERIC"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="created" column="created" jdbcType="TIMESTAMP"/>
        <result property="modified" column="modified" jdbcType="TIMESTAMP"/>
        <result property="createdBy" column="created_by" jdbcType="VARCHAR"/>
        <result property="updatedBy" column="updated_by" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List" >
        id, owner_user_id, name, brand, quantity, unit_price, total_price,
        status, created, modified, created_by, updated_by
    </sql>

    <!-- 多租户过滤：super_admin 看全部；普通用户只看自己的 -->
    <sql id="Owner_Filter">
        <if test="superAdmin == false">
            and owner_user_id = #{currentUserId}
        </if>
    </sql>

    <!--查询单个-->
    <select id="queryById" resultMap="SoapToolsMap">
        select
        <include refid="Base_Column_List" />
        from soap_tools
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SoapToolsMap">
        select
        <include refid="Base_Column_List" />
        from soap_tools
        <where>
            <if test="soapTools.name != null and soapTools.name != ''">
                and name LIKE CONCAT('%', #{soapTools.name}, '%')
            </if>
            <include refid="Owner_Filter"/>
        </where>
        order by modified desc
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from soap_tools
        <where>
            <if test="soapTools.name != null and soapTools.name != ''">
                and name LIKE CONCAT('%', #{soapTools.name}, '%')
            </if>
            <include refid="Owner_Filter"/>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into soap_tools(owner_user_id, name, brand, quantity, unit_price, total_price, status,
                               created, modified, created_by, updated_by)
        values (#{ownerUserId}, #{name}, #{brand}, #{quantity}, #{unitPrice}, #{totalPrice}, 1,
                now(), now(), #{createdBy}, #{updatedBy})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into soap_tools(name,brand, quantity, unit_price, total_price, status, created, modified, created_by,
        updated_by)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name},#{entity.brand}, #{entity.quantity}, #{entity.unitPrice}, #{entity.totalPrice}, #{entity.status},
            #{entity.created}, #{entity.modified}, #{entity.createdBy}, #{entity.updatedBy})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into soap_tools(name,brand, quantity, unit_price, total_price, status, created, modified, created_by,
        updated_by)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name},#{entity.brand}, #{entity.quantity}, #{entity.unitPrice}, #{entity.totalPrice}, #{entity.status},
            #{entity.created}, #{entity.modified}, #{entity.createdBy}, #{entity.updatedBy})
        </foreach>
        on duplicate key update
        brand = values(brand),
        quantity = values(quantity),
        unit_price = values(unit_price),
        total_price = values(total_price),
        status = values(status),
        created = values(created),
        modified = values(modified),
        created_by = values(created_by),
        updated_by = values(updated_by)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update soap_tools
        <set>
            <if test="brand != null and brand != ''">
                brand = #{brand},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="unitPrice != null">
                unit_price = #{unitPrice},
            </if>
            <if test="totalPrice != null">
                total_price = #{totalPrice},
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                updated_by = #{updatedBy},
            </if>
        </set>
        ,modified = now()
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from soap_tools
        where id = #{id}
    </delete>

</mapper>

